1+1
a <- 1
mtcars
clear
cls
clear
cls
mtcars
#data frames
mtcars$mpg
#data frames
names(mtcars$mpg)
#data frames
names(mtcars)
str(mtcars)
summary(mtcars)// retorna un primer resumen estadistico dependiendo del tipo de datos
summary(mtcars) // retorna un primer resumen estadistico dependiendo del tipo de datos
#data frames
names(mtcars) ##obtener nombres de las columnas
summary(mtcars) ## retorna un primer resumen estadistico dependiendo del tipo de datos
mtcars$cyl <- as.factor(mtcars$cyl)
summary(mtcars) ## retorna un primer resumen estadistico dependiendo del tipo de datos
View(mtcars)
setwd("/")
setwd("Users/Vicktor Sanz/Desktop/Tesis")
encuesta <- read.csv("encuesta.csv",header=T,sep = ",")
encuesta[,names(encuesta)]
names(encuesta)
encuesta <- read.csv("encuesta.csv",header=T,sep = ",",encoding ="UTF-8")
names(encuesta) %in% c("Â.CuÃ.l.es.su.edad..")
names(encuesta) %in% c("Marca.temporal")
encuesta[,names(encuesta)]
View(encuesta)
str(encuesta)
encuesta <- read.csv("encuesta.csv",header=T,sep = ",",encoding ="UTF-8")
str(encuesta)
sumary(encuesta)
summary(encuesta)
clear
clean
encuesta
